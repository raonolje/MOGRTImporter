{
  "name": "mogrt-importer-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "MOGRT Subtitle Importer 전용 MCP 서버 (stdio, 상태 없음). 패널과는 %APPDATA%/MogrtImporter/bridge 파일 다리로만 이야기한다.",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.1"
  },
  "engines": {
    "node": ">=20"
  }
}
